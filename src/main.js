/* ============================================================
   ThreadThink — Entry Point
   ============================================================ */

import { init } from './events.js';

document.addEventListener('DOMContentLoaded', init);
